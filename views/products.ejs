<% const a = '<p>Hello</p>' %>
<!DOCTYPE html>
<html lang="en">
    <head>
        <title><%= title %></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body>
        <% mang.forEach(e => { %>
            <h3><%= e %></h3>
        <% }); %>
    </body>
    <script src="/reload/reload.js"></script>
</html>
